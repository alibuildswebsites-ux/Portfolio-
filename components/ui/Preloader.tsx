
import React from 'react';

// This component has been deprecated and removed from the application flow.
// It is kept as a placeholder to prevent import errors if other files reference it,
// though all references should have been removed.
const Preloader: React.FC<any> = () => null;

export default Preloader;
